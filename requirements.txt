Django==5.2.13
djangorestframework>=3.15.0
langchain-together>=0.3.0
langgraph>=0.4.1
langchain-core>=0.3.40
pydantic>=2.11.0
pymongo>=4.12.0
python-dotenv>=1.1.0
uvicorn[standard]>=0.34.0
# stories_v2 only needs httpx (already a transitive dep of django-rest-framework
# clients but we pin explicitly so the provider router has a known floor).
httpx>=0.27.0
certifi>=2024.7.4
django
gunicorn